{
  "name": "paralegal-toolkit-extension",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "build": "npm run build:lib && npm run build:ts && npm run build:viewer",
    "build:lib": "node copy-pdfjs.cjs",
    "build:ts": "tsc -p tsconfig.json",
    "build:viewer": "esbuild src/viewer.ts --bundle --format=esm --outfile=scripts/viewer.js --minify",
    "package": "npm run build && node copy-to-dist.cjs",
    "package:zip": "npm run package && powershell -NoProfile -Command \"Compress-Archive -Path dist -DestinationPath paralegal-toolkit-bates-stamp.zip -Force\""
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.28.0",
    "pdfjs-dist": "^4.0.379",
    "typescript": "~5.6.0"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}